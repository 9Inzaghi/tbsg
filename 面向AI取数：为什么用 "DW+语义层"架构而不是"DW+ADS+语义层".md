# 面向AI取数：为什么用 "DW+语义层"架构而不是"DW+ADS+语义层"

当核心目标明确为"**面向 AI 取数**"（即 Text-to-SQL 或 Text-to-Metrics）时，架构的选择直接决定了 AI 的**准确率、回答范围和可维护性**。

### 一、核心区别：AI 视角的差异

在"AI 取数"这个特定场景下，两种架构的本质区别在于：**AI 是面对一个"死板的菜单"，还是面对一本"活的百科全书"**？

| 维度 | **架构 A：DW + ADS + 语义层**<br>(传统三层) | **架构 B：DW + 语义层**<br>(现代扁平化) |
|------|-------------------------------------------------------|---------------------------------------------------|
| **AI 的视野范围** | **受限**。AI 只能看到 ADS 层预定义好的指标和维度组合。如果用户问的问题超出了 ADS 表的预设范围，AI 会回答"不知道"或产生幻觉。 | **全域开放**。AI 可以看到 DWD/DWS 的所有原子字段和维度，能自由组合回答任何长尾问题（Ad-hoc）。 |
| **上下文复杂度**(Context) | **低但僵化**。每个 ADS 表是一个独立的上下文。AI 需要在几十个 ADS 表中选择正确的表，容易选错（Schema Linking 错误）。 | **结构化且统一**。语义层提供统一的元数据图谱。AI 只需理解一套逻辑模型，通过关联关系自动推导，减少了"选表"的错误率。 |
| **动态推理能力** | **弱**。无法处理"按新维度拆解"的需求（例如：ADS 只有"按天"，用户问"按小时"）。 | **强**。支持任意维度的动态 Group By 和过滤，完美契合 AI 的探索性提问。 |
| **口径一致性风险** | **高**。不同 ADS 表可能对同名指标有不同计算逻辑，AI 难以分辨细微差别，导致回答矛盾。 | **极低**。语义层强制单一事实来源（SSOT），AI 调用的永远是同一套公式。 |
| **维护与迭代** | **慢**。业务变了 -> 改 ETL -> 重跑 ADS -> 更新语义层。AI 响应滞后。 | **快**。业务变了 -> 改语义层配置 -> 即时生效。AI 立刻能学会新逻辑。 |
| **对 LLM 的要求** | **高**。需要 LLM 具备极强的"表路由"能力，从众多相似表中选出唯一正确的那张。 | **中**。LLM 只需要理解业务意图，路由逻辑由语义层引擎自动处理。 |

#### 结论

对于 **AI 取数**，**架构 B (DW + 语义层) 是绝对的最优解**。
架构 A 中的 ADS 层实际上是 AI 的"枷锁"。AI 的核心价值在于处理**未预设的、灵活的**问题，而 ADS 层的设计初衷恰恰是**固化**查询路径。保留 ADS 层会让 AI 退化为一个只会查固定报表的机器人，失去了智能分析的意义。

---

### 二、从"DW+ADS+ 语义层"转型为"DW+ 语义层"的建设路径

要去掉 ADS 层，让 AI 直接基于 DW 和语义层工作，你需要完成以下五大核心建设：

#### 1. 数据模型"宽表化"与"原子化"改造 (Data Modeling)

ADS 层通常承载了大量的 Join 和预处理逻辑。去掉它后，这些逻辑必须上移或简化。
- **动作**：
    - **构建主题域宽表 (Subject-Oriented Wide Tables)：在 DWD/DWS 层，将事实表与常用维度表进行维度退化**（Denormalization）。确保 AI 查询时，80% 的场景不需要跨表 Join。
    - **提取原子指标**：检查所有 ADS 表中的计算逻辑，将其拆解为最基础的原子字段（如 `amount`, `cost`, `status`），存入 DWS。
    - **清洗前置**：确保 DWD/DWS 中的数据已经是干净的（无 NULL 陷阱、枚举值标准化），因为不再有 ADS 层做最后的"美容"。

#### 2. 语义层"逻辑全覆盖" (Semantic Logic Migration)

这是最关键的一步。原本写在 ADS ETL SQL 里的复杂业务逻辑（如转化率、同环比、特定过滤规则），必须全部迁移到语义层的配置中。
- **动作**：
    - **指标公式迁移**：将 `ads_table.profit_rate` 的计算逻辑 `(sum(rev)-sum(cost))/sum(rev)` 提取出来，定义为语义层的**复合指标**。
    - **时间智能内置**：利用语义层引擎的能力（如 dbt-metrics, Cube.js），原生支持 `YoY`, `MoM`, `Rolling_7d` 等计算，不再依赖 ADS 预算。
    - **动态过滤规则**：将 ADS 表中硬编码的 `WHERE status = 'active'` 转化为语义层的**默认过滤器**或**权限策略**。

#### 3. 引入/强化"智能物化视图" (Performance Optimization)

去掉了 ADS 的预计算结果表，AI 的动态查询可能会变慢。需要用技术手段弥补性能。
- **动作**：
    - **启用 OLAP 引擎的物化视图**(Materialized Views)：在 StarRocks/Doris/ClickHouse 中，针对高频查询模式（如"按天 + 地区"聚合）创建物化视图。
    - **透明加速**：配置引擎的**查询重写**(Query Rewrite) 功能。当 AI 生成 SQL 查询大宽表时，引擎自动判断并路由到物化视图，**对 AI 和语义层完全透明**。
    - **效果**：既保留了 AI 的灵活性（可以查任意维度），又获得了 ADS 层的查询速度。

#### 4. 构建 AI 友好的元数据增强 (Metadata Enrichment for AI)

没有 ADS 表作为中间缓冲，AI 直接面对庞大的 DW 模型，需要更好的"导航"。
- **动作**：
    - **丰富业务描述**：在语义层中，为每一个字段、指标、维度编写详细的 `Description`（自然语言描述）。告诉 AI："这个字段代表什么业务含义，什么时候该用它"。
    - **建立知识图谱**：在语义层显式定义实体间的关联关系（Joins），帮助 AI 理解如何跨主题查询。
    - **Few-Shot 示例库**：在语义层存储一些标准的"问题 -SQL"对，作为 AI 生成的参考范例（Prompt Context）。

#### 5. 治理流程与验证机制 (Governance & Validation)
- **动作**：
    - **双轨运行比对**：在过渡期，并行运行旧 ADS 链路和新语义层链路。自动对比同一问题下的数据结果，确保去 ADS 化没有导致数据偏差。
    - **查询熔断与限流**：由于 AI 可能生成低效 SQL，必须在网关层或数据库层设置资源配额和超时熔断，防止单个 AI 请求拖垮集群。
    - **反馈闭环**：建立机制，当 AI 回答错误或查询超时时，自动触发告警，提示数据团队优化语义层定义或底层模型。

### 三、转型后的预期收益

完成上述建设后， AI 取数能力将发生质变：
1. **回答范围扩大 10 倍**：从只能回答"预设好的 50 个问题"，变成能回答"基于数据的任意问题"。
2. **准确率提升**：消除了"选错 ADS 表"的风险，统一了口径，AI 不再胡说八道。
3. **响应速度加快**：业务今天定义新指标，明天 AI 就能用，无需等待 ETL 开发排期。
4. **成本降低**：减少了大量 ADS 表的存储成本和 ETL 计算资源消耗。

### 总结建议

**坚决去掉 ADS 层**。
在 AI 时代，ADS 层是阻碍智能化的最大绊脚石。
建设重心应放在：**打造极致的 DWD/DWS 宽表** + **构建强大且描述丰富的语义层** + **利用 OLAP 引擎的自动加速能力**。

这将使数据架构从"为报表服务"真正转变为"为智能服务"。
